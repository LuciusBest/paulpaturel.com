This folder contains internal QA utilities for the COSMA type tester.

Notes
- Files here are not referenced by index.html and are not served publicly.
- To run the harness manually, copy the desired script into js/ and add a script tag.
- Remove it again when finished to avoid shipping test code.

