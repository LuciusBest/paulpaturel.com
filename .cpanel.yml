---
deployment:
  tasks:
    - export DEPLOYPATH="$HOME/public_html"
    - /bin/mkdir -p "$DEPLOYPATH"
    - /bin/rsync -av --delete \
        --exclude='.git*' \
        --exclude='.cpanel*' \
        --exclude='.well-known/' \
        --exclude='cgi-bin/' \
        public/ "$DEPLOYPATH"/
